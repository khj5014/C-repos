#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <Windows.h>

//숫자받기함수
int get_int(char* text);
int get_menu(char* text);
int get_MVP(char* text);

//템선택
void jcube();
void Mcube();